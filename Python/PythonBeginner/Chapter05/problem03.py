# Can we have a set with 18 (int) and '18' (str) as a value in it?

s1= set()
s1.add(18)
s1.add("18")
print(s1)

# so answer will be yes we can have both int and str type values in set as set is 
# a collection of heterogeneous data types

